import { Route } from '@angular/router';
import { AppointmentSchedulerComponent } from './appointment-scheduler.component';

export const appointmentSchedulerRoutes: Route[] = [
    {
        path: '',
        component: AppointmentSchedulerComponent,
    }
];